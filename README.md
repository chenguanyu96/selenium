# Selenium with Facebook Messenger
A small application that can automatically message someone on Facebook Messenger (Web Only).

# Running the program
Python 3.6 required with Selenium package installed.
